# The API origin. ORIGIN ONLY — the client appends /api/v1.
# Both are NEXT_PUBLIC_*, so they are inlined at BUILD time and must be set
# when the app is built, not only when it runs.
NEXT_PUBLIC_API_URL=http://localhost:8010
NEXT_PUBLIC_WS_URL=ws://localhost:8010
