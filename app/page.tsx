import { LandingPage } from "@/features/marketing/components/landing-page"

export const metadata = {
  title: "Prompt Studio — draw the app, get the prompt that builds it",
  description:
    "Turn a diagram of your product into the build prompt a coding agent needs. Plus a free library of prompts for design, debugging, review and planning.",
}

export default function Page() {
  return <LandingPage />
}
